/* Optional Supabase connection point.
   Add your project values after creating a Supabase project.
   The current demo intentionally uses localStorage so it works without credentials. */
window.TUTORSPHERE_SUPABASE = {
  url: "",
  anonKey: "",
  enabled: false
};
